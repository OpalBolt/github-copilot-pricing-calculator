# Use separate router offers and published router prices

The calculator shows one Router Model Offer for each router-specific model listing. It does not merge equivalent models across routers. Each estimate uses the model-level Router Price published for the active routing mode. This keeps the static build simple and avoids extra requests for every provider endpoint. A Router Price is an advertised comparison price, not a quote for a specific inference backend.
